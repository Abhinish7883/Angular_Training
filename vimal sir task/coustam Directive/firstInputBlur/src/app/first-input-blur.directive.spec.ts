import { FirstInputBlurDirective } from './first-input-blur.directive';

describe('FirstInputBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstInputBlurDirective();
    expect(directive).toBeTruthy();
  });
});
